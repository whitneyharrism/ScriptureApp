<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.scripturestudyapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PopNotification"></activity>
        <activity android:name=".QuizResults"></activity>
        <activity android:name=".LessonPlanner" />
        <activity android:name=".Profile" />
        <activity
            android:name=".LessonPage"
            android:label="@string/title_activity_lesson_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".QuizPage"
            android:label="@string/title_activity_quiz_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainPage" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>